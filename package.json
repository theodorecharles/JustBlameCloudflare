{
  "name": "just-blame-cloudflare",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "A small server for a simulated Cloudflare error page.",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node start.js",
    "test": "node --test"
  }
}
